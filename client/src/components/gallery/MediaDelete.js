import React from 'react';

const MediaDelete = ({ onDelete }) => {
    // Implement deletion logic
    return (
        <button onClick={onDelete}>Delete</button>
    );
};

export default MediaDelete;
